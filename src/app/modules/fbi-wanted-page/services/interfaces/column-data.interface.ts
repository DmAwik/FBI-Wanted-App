export interface Filters {
  title: string;
  placeOfBirth: string;
  sex: string;
  race: string;
  weight: string;
  nationality: string;
}

export interface ColumnsData {
  label: string;
  property: string;
  visible: boolean;
}
